# Type: AspynotificationsWorkerParams
events_archive_worker:

  # Type: CloudEventsWorkerConfig
  nats_worker:
    subscriptions: {{ (g_app_aw_nats_worker_subscriptions | default('events.>')).split(',') if (g_app_aw_nats_worker_subscriptions | default('events.>')) != '' else [] }}
